# motion_force_simulator component configuration

# Global gravity vector (m/s^2)
gravity_vector = [0.0, -9.81, 0.0]

# Default max force for generic actuators in Newtons
default_actuator_max_force = 1000.0

# Default spring constant for joint constraints in N/m
default_joint_spring_constant = 5000.0
